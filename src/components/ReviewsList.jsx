import React from 'react';


const ReviewsList = () => {
  const {useState, useEffect} = React;
  const [reviews, setReviews] = useState([]);
  const [reviewCount, setReviewCount] = useState(0);

  return (

  )
};

export default ReviewsList;

//display two tiles at a time
  //max height should be capped
  //scrollable

//More Reviews Button
  //render when 2 or more reviews
  //not render when 2 or less reviews
  //clicking will cause 2 more reviews to appear


//sort and dropdown buttons should be fixed outside


//list of reviews
  //render first 2 reviews
